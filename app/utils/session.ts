// app/utils/session.ts
/* import { getSession } from "~/services/session"; // Modify based on your session implementation

export async function getSessionUser(request: Request) {
  const session = await getSession(request.headers.get("Cookie"));
  return session.get("user") || null; // Adjust based on your session data structure
}
*/
